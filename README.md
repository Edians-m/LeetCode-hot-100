# LeetCode Hot 100 刷题记录
语言：C++
进度：已完成 8 题



一、哈希
    1、两数之和             （简单）    【遍历，对当前 nums[i]，检查 target - nums[i] 是否在哈希表中】
    2、字母异位词分组       （中等）    【还没写】


二、双指针
    283、移动零             （简单）   【快慢指针，快指针遍历数组，遇到非零元素则与慢指针交换，然后慢指针前进1步】
    11、盛最多水的容器      （中等）    【左右指针从两端向中间移动，每次计算当前容器面积并更新最大值，移动高度较小的指针】  （贪心）
    15、三数之和            （中等）    【先排序，从头遍历，对每个 nums[i]，使用双指针在它右侧从两端往中间寻找两数之和等于 -nums[i]，注意：一定要跳过重复值】

三、普通数组
    53、最大子数组和        （中等）    【动态规划，cur来装a[i]和cur+a[i]的最大值,best来装整个过程cur的最大值】
    56、合并区间            （中等）    【先按区间左端点升序排序。如果当前区间左端点大于结果数组中最新一个区间的右端点，则直接加入，否则合并（更新右端点）】
    189、轮转数组           （中等）    【复制数组、数组翻转    还有个方法：环状替换，目前还没学】

四、滑动窗口
    3、无重复字符的最长子串  （中等）    【滑动窗口+数组记录字符最新下标，右指针遍历，若当前字符上次出现位置≥left，则left更改为该位置加一，每步计算窗口长度并更新最大值】